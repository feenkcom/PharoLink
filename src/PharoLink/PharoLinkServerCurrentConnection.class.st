"
PharoLinkServerCurrentConnection holds the connection that is processing the current request, enabling it to be accessed during processing.
"
Class {
	#name : #PharoLinkServerCurrentConnection,
	#superclass : #DynamicVariable,
	#category : #'PharoLink-Server'
}
